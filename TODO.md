# TODO List

- [x] Documentation is lacking features and is not updated: Read the content of the transpiler and the grammar and update SYNTAX.md, TRANSPILATION.md,  docs/server_plugins.md, docs/stdlib.md
- [x] Add to stdlib the actor model for concurrency It could take inspiration by https://github.com/vladopajic/go-actor, implement it natively in lammergeier
- [x] Verify that the http server support multipart upload, if not implement it
- [x] It should be possible to use named arguments for functions
- [x] Implement destructuring of dictionaries too, like the one in js
- [x] If it isn't yet, make the slicing more powerful like the python one
- [x] If they don't yet, allow f strings to enable generic code expressions inside parenthesis like: f"user={users[0].profile?.email ?? "none"}", manage correctly the apixes, mine is just an example
- [x] Create a migration system like the knex js one, it would be nice to have a cli command to generate migrations and run them. The connection can be specified via command line, and the type of database too (mysql, postgres, sqlite, etc.)
- [x] If not present add a query builder system in the db stdlib module, if present make it rich. Also if possible add a way to do transactions too and rollbacks (They have to have a clear behaviour in case of error too: The transaction must be rollbacked or there's an automatic rollback?)
- [x] Add to stdlib a redis and a memcached module (you can launch them via docker run --rm, I want them to work both via auth and without authentication, use go library if necessary under the hood)
- [x] Standard lib for dataframes like pandas: new `lamdata` stdlib wrapping [`github.com/go-gota/gota`](https://github.com/go-gota/gota). Ships `DataFrame` (construction via `fromRecords` / `fromMaps` / `readCSV` / `readJSON`; subsetting / filtering / sorting / joins / rbind / cbind / concat / describe / groupBy; typed `mutateInts` / `mutateFloats` / `mutateStrings` / `mutateBools` alongside the generic `mutate`; `writeCSV` / `writeJSON` I/O), `Series` (typed constructors + full stats surface: `sum` / `mean` / `median` / `stddev` / `min` / `max` / `quantile` plus `toFloatList` / `toIntList` / `toStringList` / `toBoolList`), and `DataFrameGroups` (`aggregate` over `max`/`min`/`mean`/`median`/`std`/`sum`/`count`, plus `getGroups()`). Errors are surfaced via `.error()` on each wrapper rather than via panics. See `lib/lamdata.lam`, `tests/tests/cases/stdlib/test_stdlib_lamdata.lam` (27 assertions), and the `lamdata` section of `docs/stdlib.md`.
- [x] Standard library for cron scheduling
- [x] Standard library for email management (Like smtp, etc.)
- [x] Standard library to read environment variables and config files (like .env, .toml, .json, etc.) --> Json, yaml, xml and maybe csv too, are already present
- [x] In the http server add cookies management too if not present, it needs to be a fully functional http server with all the features of a modern http server, use plugins when necessary. The web server implementation should have modern features at a 360° angle, not only about cookies. Make the the http server super powerful, and use the functionalities of the golang http module when you can to avoid reimplementing the wheel, the wrapper must be lammergeier friendly and have a modern syntax, it should resemble fastify as you remember
- [x] LSP doesn't flag the newly-supported syntax anymore. Added `LAMMERGEIER` to the semantic checker's `BUILTIN_CONSTANTS` so attribute access on it (`LAMMERGEIER.userClass(args)`) no longer trips the undefined-name guard (the post-parse user-name resolver rewrites the tail at emit time). Expanded `STDLIB_MODULES` to include every stdlib module currently shipped under `lib/` (incl. `lamdata`, `lamarray`, `lamserver*`, `lamactor`, `lamredis`, `lamcron`, `lamsmtp`, `lamjwt`, etc.). Probed `_ = sideEffect()`, `LAMMERGEIER.<userName>`, and `lamdata` imports via a live LSP session — all three produce zero diagnostics. LSP integration suite stays green (`python3 tests/lsp/test_lsp.py`).
- [x] Docs refreshed to match the recent changes. `SYNTAX.md` gains a **Discarding a value with `_`** subsection under *Variables and Constants* and clarifies that `LAMMERGEIER.<name>` typo detection now runs during semantic checking (not as a preprocessor textual pass). `TRANSPILATION.md` gains a **Discard target (`_`)** subsection under *Variables* documenting the `_ = expr` → plain `=` lowering. `README.md` advertises the new pandas-style `lamdata` stack alongside `lamarray`, and the SYNTAX.md stdlib inventory table adds the `lamdata` row (`DataFrame` / `Series` / `DataFrameGroups`, go-gota backend, full method checklist).
- [x] LAMMERGEIER.* now also resolves user-side names. Compiler-emitted helpers (`Result.Ok`, `Error`, `nil`) still get a textual rewrite before parsing; **user-defined top-level functions / classes / `Class.staticMethod` paths** are resolved post-parse via a new `_resolve_user_lammergeier` helper that runs at go-block emit time, after `_collect_function_names` populates the symbol tables. The typo guard now runs on the deferred name set so user names participate in did-you-mean suggestions. `LAMMERGEIER.MyClass(args)` lowers to `NewMyClass(args)` (constructor) while `LAMMERGEIER.MyClass` (no trailing call) gives the type. Limitation: only declared identifiers — literals / expressions / local vars are out of scope. See `tests/tests/cases/test_lammergeier_user_alias.lam` and the `LAMMERGEIER.*` section of `SYNTAX.md`.
- [x] migrate_cli functionalities should be part of the "lamc" command as sub parameters
- [x] Unified `install.sh` at the project root (plus a matching `lamc install`-free workflow) installs the compiler + LSP launcher via symlinks. Auto-picks `/usr/local/bin` if writable, else `~/.local/bin`; `--user` / `--system` / `--prefix DIR` override. Prereq checks (Python 3.10+, Go 1.21+, `lark`), optional `--with-editor [vscode|windsurf|cursor|all]` delegates to `vs-code-extension/install.sh` for the editor integration, `--dry-run` previews. To make the symlinks robust the launchers were made symlink-safe: `lamc` resolves its root via `os.path.realpath`, and `bin/lammergeier-lsp` follows `BASH_SOURCE` through `readlink -f` (with a portable fallback). See the Quick start section of `README.md`.
- [x] LSP + editor-extension installation is covered by the same `./install.sh` (no separate command needed). `install.sh --with-editor all` runs `vs-code-extension/install.sh` after symlinking `bin/lammergeier-lsp`, so one command bootstraps both the language server and the VS Code / Cursor / Windsurf extension.
- [x] Test / benchmark directory organisation tightened. `lamc` now defaults the output binary to the **source-adjacent path** (`lamc src/foo.lam` → `src/foo`) instead of the CWD stem, so ad-hoc invocations don't pollute the project root. The test and benchmark harnesses already used `tempfile.TemporaryDirectory` to keep their artefacts scoped; the change only affects manual / developer invocations. `.gitignore` was extended with patterns covering the new layout (`tests/tests/cases/**/test_*` sans `.lam`, `tests/benchmarks/cases/**/bench_*` sans `.lam`, plus `/lamsmtp_*` and `/lam_*` catch-alls for any stray root binaries). Existing stray ELF binaries at the project root (28 of them, left over from earlier manual runs) were cleaned up.
- [x] Prepare the compiler to look for libraries also in the folder "extlibs", the compiler will look first in the stdlib, then here, then in the project that's compiling directories.The extlibs folder will become the place third party libraries will be placed
- [x] Create a mark down file proposal for third party library distribution and installation
- [ ] Increase stdlibraries internal coverage, so try to complete more the stdlibs already present. Http web server should become very well implemented and fully sugared, I want to use lammergeier for my new project and replace my old node js server which is using fastify js
      - [x] Fastify-parity audit of `lamserver` + new sugar: `res.getHeader/getHeaders/hasHeader/removeHeader/hijack/elapsedTime`, `req.protocol/hostname/ips/routeOptions/startTime`, `srv.addSchema/removeSchema`, `$ref` resolution in per-route schemas, `srv.setSchemaErrorFormatter`, `srv.onRequestAbort` (see `docs/stdlib.md` lamserver section and `tests/tests/cases/stdlib/test_stdlib_server_fastify_sugar.lam` + `test_stdlib_server_on_request_abort.lam`).
      - [x] Request timeouts + `onTimeout` hook (dispatcher wraps handler in `context.WithTimeout`; per-route `timeoutMs` override; see `tests/tests/cases/stdlib/test_stdlib_server_request_timeout.lam`).
      - [x] Streaming request bodies: new `RequestBodyReader` class + `req.bodyReader()` (works on buffered routes too via `strings.NewReader`), per-route `streamBody: true` opt-in in route opts; see `tests/tests/cases/stdlib/test_stdlib_server_stream_body.lam`. Compiler fix in `compiler/visitors/expressions.py`: file-sugar rewriting of `.read` / `.write` / `.close` / `.readline` / `.readlines` now skips receivers that are known user class instances (or `self` inside a class body) so user classes can define their own `read`/`close`/etc. without colliding with the built-in `io.ReadAll` transform.
      - [x] `reply.serialize()` standalone + `printRoutes()` CLI-friendly formatter. `Response.serialize(payload=None)` applies the matched route's response schema (`res.ctx["__lamMatchedRoute"]`); `Server.printRoutes()` returns a sorted `METHOD /path [flags] key=value` table with `[stream]` / `[deprecated]` / `[hidden]` / `[timeout=...]` flags. See `tests/tests/cases/stdlib/test_stdlib_server_serialize_printroutes.lam`.
      - [x] Dataframes stdlib — see the standalone `lamdata` entry above.
      - [x] Second Fastify-parity pass on `lamserver`: `req.routerPath()` / `req.routerMethod()` / `req.is_(type)` / `req.url()`; `res.setHeaders({...})` (batch header writer — `headers` itself is a struct field so we don't shadow it) / `res.getResponseTime()` (alias for `elapsedTime`) / `res.callNotFound()` (delegate to the registered `notFoundHandler` — dispatcher honours the `__lamCallNotFound` ctx flag set by the reply and re-dispatches the NF pipeline); `srv.hasRoute(method, path)` (exact-pattern presence check) and `srv.useSecurityHeaders(opts={})` (Helmet-equivalent: CSP, HSTS, X-Content-Type-Options, X-Frame-Options, Referrer-Policy, COOP/CORP, X-DNS-Prefetch-Control, X-Download-Options, X-Permitted-Cross-Domain-Policies, Origin-Agent-Cluster, optional Permissions-Policy — every header overridable, never clobbers one the handler already set). See `tests/tests/cases/stdlib/test_stdlib_server_fastify_more.lam` (342 tests pass).
      - [x] Cross-library static-method resolution. Compiler bug: when a user library (`greet_kit.lam`) imported another library (`lamstrings`) and called its static methods (`Strings.toUpper(...)`), the per-library transpiler emitted the bare Go attribute access `Strings.ToUpper(...)` because its `_class_names` / `_static_methods` only knew about classes defined in the same file. Fix in `compiler/lammergeier.py`: pre-scan every library tree up-front (reusing the same walk that discovers transitive imports) to build a union `lib_pre_class_names` + `lib_pre_static_methods`, then inject both into each per-library `GoTranspiler` before `transpile()`. `compiler/cache.py` grows an `extra` discriminator on `cache_key` / `load` / `save` so the per-library on-disk cache is scoped by the cross-library context (no stale hits when a new sibling library introduces a class). Regression coverage in `tests/tests/test_extlibs_resolution.py` (11 tests, including a custom library at an unrelated absolute path that imports `lamstrings` and calls `Strings.toUpper`).


      - [x] Third-party package manager. `lamc install <name[@version]>` / `<git-url>` / `<path>` fetches a library, validates its `lamlib.toml`, and copies it to `~/.lammergeier/extlibs/<name>` (or `./extlibs/` with `--vendor`, which also writes a deterministic `lamlib.lock.toml`). `lamc publish` packs the source tree (rooted at `<name>-<version>/`) and POSTs it to the registry; `lamc uninstall` removes it. Source-only distribution — no compiled artefacts ship across machines, so the same tree compiles cleanly on every OS / arch the host's Go can target. Scoped names (`@scope/name`) round-trip through the resolver, the install path (`extlibs/@scope/name/__init__.lam`), and the registry's URL handling. SemVer / API-diff gate refuses a `0.1.x → 0.1.y` "patch" upgrade that secretly drops a public function (rerun with `--allow-breaking` to override) — implemented via `compiler/apidiff.py` (AST-based extraction of top-level funcs, classes, methods, fields). Reference registry under `tools/registry/` ships as a self-contained Python service plus a `Dockerfile` + `docker-compose.yml` (seed catalogue: `lamgreet@1.0.0`, `@acme/lamcolor@0.2.0`); zero runtime deps, three endpoints (`GET /api/v1/libraries/<name>`, `GET …/<name>/<file>.tar.gz`, `POST /api/v1/publish`). Coverage: `tests/tests/test_manifest.py` (11 tests), `test_apidiff.py` (13), `test_scoped_imports.py` (6), `test_install_cli.py` (7 — runs the registry in-process). Docs: `docs/third_party_libraries.md`.
      - [x] Transitive dependency resolution with cross-library + cross-project conflict detection, covering both Lammergeier libs and the Go modules that libraries import via `go { }` blocks. `lamc install X` now walks the full `[dependencies]` graph of `X`, folds the project's own `lamlib.toml` plus every already-installed lib's constraints into a single demand set, picks the highest non-yanked registry version that satisfies *all* demands, and installs leaves-first so a mid-resolution conflict never leaves half-installed libs on disk. `[go-deps]` / `[go.dependencies]` in `lamlib.toml` pin Go modules per library; incompatible majors of the same Go path across libs + project are a hard error (Go treats each major as a different package), compatible pins merge via Go's MVS rule. The resolved Go pins land in the lockfile under `[go_pins.<module-path>]` and are injected as `require` directives into the compile-time synthesised `go.mod` so `go mod tidy` honours them instead of picking the newest tag at random. New helpers: `compiler/manifest.py` gains `_GO_MODULE_PATH_RE` / `_GO_VERSION_RE`, `go_version_tuple` / `go_major` / `is_valid_go_module_path` / `is_valid_go_version`, and a quote-aware `_split_table_path` so `[go_pins."github.com/foo/bar"]` round-trips. `compiler/install_cli.py` gains `DependencyConflict` / `ResolvedDep` and the `_walk_dependencies` breadth-first resolver that folds project + installed + freshly-fetched manifests. `compiler/lammergeier.py` gains `_collect_go_pins` + `_inject_go_requires` (runs between `go mod init` and `go mod tidy`). Coverage: `tests/tests/test_dependency_crash.py` (9 scenarios: transitive auto-install, two-libs-incompatible-majors, project-vs-lib overlap success + conflict, Go-deps compatible MVS, Go-deps incompatible majors, project-vs-lib Go overlap, lockfile round-trip into `go.mod`), plus 3 new manifest tests (14 total).


# Future activities, not to be implemented yet