# Test: list comprehension with string type
# expect: [HELLO WORLD FOO]

func main() {
    words: list[str] = ["hello", "world", "foo"]
    upper: list[str] = [w.toUpper() for w in words]
    print(upper)
}
