# Test: List comprehension with string type
# expect: HELLO
# expect: WORLD
# expect: 2

func main() {
    words: list[str] = ["hello", "world"]
    for w: str in words {
        print(w.toUpper())
    }
    print(len(words))
}
