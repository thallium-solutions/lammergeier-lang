# expect-error: destructuring expects 3 values, but `splitPair()` has 2

from destructuring_package import splitPair

func main() {
    left, right, extra = splitPair("a:b")
    print(left)
    print(right)
    print(extra)
}
