# expect-pass

func main() {
    for i in range(2) {
        func read(i: int) -> int {
            return i
        }
        fn = lambda (i: int): i + 1
        print(read(i))
        print(fn(i))
    }
}
