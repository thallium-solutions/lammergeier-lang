# expect-error: ambiguous inherited member `label` on class `Both`

from class_inheritance_helper import LabelBase, Left, Right

class Both(Left, Right) {
    func value(self) -> str {
        return self.label()
    }
}

func main() {
    both: Both = Both()
    print(both.value())
}
