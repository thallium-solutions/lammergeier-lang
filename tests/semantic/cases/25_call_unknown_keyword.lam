# expect-error: unknown keyword argument `punctuatio`
# expect-error: did you mean `punctuation`

func greet(name: str, punctuation: str = "!") -> str {
    return name + punctuation
}

func main() {
    print(greet("Lam", punctuatio="?"))
}
