# expect-error: requires unaliased `from lamerrors import Result`
# expect-error: `Result as ...` is not supported

from lamerrors import Result as R

func main() {
    do {
        print("ok")
    } catch err {
        print(err)
    }
}
