# expect-error: destructuring expects 3 values, but `maker.instancePair()` has 2

from tuple_return_helper import Maker

func main() {
    maker: Maker = Maker()
    a, b, c = maker.instancePair()
    print(a)
}
