# Transpilation: Go-only keywords are allowed when Lam declarations lower to safe Go names.
# expect-go: func Select() int {
# expect-go: type QueryBuilder struct {
# expect-go: Chan int `json:"chan"`
# expect-go: func (s *QueryBuilder) Select() int {
# expect-go: func QueryBuilder_switch(value int) int {
# expect-go: return s.Chan

func select() -> int {
    return 1
}

class QueryBuilder {
    chan: int

    func select(self) -> int {
        return self.chan
    }

    static func switch(value: int) -> int {
        return value
    }
}

func main() {
    builder: QueryBuilder = QueryBuilder()
    print(select())
    print(builder.select())
    print(QueryBuilder.switch(3))
}
